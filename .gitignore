# 의존성
node_modules/

# 환경변수·비밀 (API 키는 절대 커밋 금지)
.env
.env.*
.dev.vars
*.key

# 빌드 산출물 / 벡터 인덱스 (재생성 가능)
dist/
build/
*.sqlite
*.db
index/vectors/

# OS / 에디터
.DS_Store
Thumbs.db
.vscode/
.idea/

# corpus 검색 인덱스 (build-index.mjs로 재생성 → KV 업로드. 번들/커밋 불필요)
chatbot/worker/corpus-index.json

# Cloudflare Wrangler 로컬 캐시
chatbot/worker/.wrangler/

# region/ 의 생성물 — 원천 CSV(약 18MB)와 빌드 결과물.
# 배포 전에 fetch → build 로 항상 새로 만든다(통계가 매월 갱신되므로 낡은 사본이 더 위험하다).
region/data/
